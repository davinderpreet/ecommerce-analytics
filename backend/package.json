{
  "name": "ecommerce-analytics-backend",
  "version": "1.0.0",
  "private": true,
  "description": "LDAS E-commerce Analytics – backend (Shopify first, BestBuy later)",
  "main": "dist/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "nodemon --watch src --ext ts,js --exec ts-node src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",

    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate deploy",

    "vercel:build": "npm ci && npx prisma generate && npx prisma migrate deploy && npm run build"
  },
  "dependencies": {
    "@prisma/client": "^5.3.1",
    "@vercel/node": "^3.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "node-fetch": "^3.3.2",
    "node-cron": "^3.0.3",
    "memory-cache": "^0.2.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/node": "^20.5.0",
    "nodemon": "^3.0.1",
    "prisma": "^5.3.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
