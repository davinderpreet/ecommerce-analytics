{
  "version": 2,
  "buildCommand": "npm ci && npx prisma generate && npx prisma migrate deploy && npm run build",
  "outputDirectory": ".",
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs20.x",
      "memory": 512,
      "maxDuration": 60
    }
  }
}
