{
  "name": "ecommerce-analytics",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "engines": {
    "node": "18.x",
    "npm": "8.x"
  },
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npx serve -s build -p $PORT"
  }
}
